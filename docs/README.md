# ijkplayer 现代化文档索引

- `upgrade/`：过程文档（按阶段记录改动与验证）。
- `knowledge_base/`：长期有效知识（仅收录构建验证过的结论）。
- `handoff/`：会话交接（每会话结束更新）。
- 计划源：`.kilo/plans/1788505926274-ijkplayer-modernization-plan.md`（实现唯一 source of truth）。
