## 任务：知识库新增 SDK 接入者文档 + 功能支持矩阵

事实收集已完成（两轮 Explore：module 配置精确清单 + SDK 公共 API 清单）。执行内容：

### 1. 新增 `docs/knowledge_base/sdk-user-guide.md`（SDK 接入指南）
- 模块职责与 Gradle 依赖引入（arm64/x86_64 按 ABI 引入、exo 可选）
- native 库加载（loadLibrariesOnce / IjkLibLoader）
- 最小接入代码两种方式（直接 IjkMediaPlayer / example 的 IjkVideoView）
- 数据源五重载 + IMediaDataSource/IAndroidIO 自定义
- 8 个事件监听器 + 常用 MEDIA_INFO/ERROR 常量 + 统计 getter
- setOption 四类 category 与常用选项表（mediacodec/opensles/accurate-seek/缓冲水位/soundtouch 等）
- ijkio 边下边播缓存用法
- 多后端切换（IjkMediaPlayer/IjkExoMediaPlayer/AndroidMediaPlayer 同 IMediaPlayer）
- Track 选择、ProGuard（无需额外 keep）、调试

### 2. 新增 `docs/knowledge_base/media-capabilities.md`（核心功能支持矩阵）
基于 config/module-lite.sh 与实际构建 config.mak（已核实一致）：
- 容器/解封装：mp4/mov、HLS(m3u8)、flv/live_flv、mpegts/mpegps、mp3、aac、flac、concat、data、hevc、webm_dash_manifest、ijk* 协议封装
- 解码：软解 aac/aac_latm/flv/h264/mp3*/vp6f/flac/hevc/vp8/vp9；硬解 MediaCodec 走 ijk 自带管线（h264/hevc/vp8/vp9），非 FFmpeg mediacodec
- parsers：aac/aac_latm/h264/flac/hevc
- 协议：默认全开减显式禁用（http/https(openssl)/file/rtmp/rtmpt/ffrtmphttp/async/ijkio/tcp/udp 可用；rtp/srtp/concat 协议/crypto/subfile 等被禁用）
- 复用/编码能力边界：muxer 仅 mp4、encoder 仅 png（非转码/录制工具）
- bsf 与档位差异（lite / lite-hevc / default）
- 播放器特性汇总（accurate seek、变速不变调、ijkio 缓存、dns cache、TCP fastopen 等）
- 每节标注来源与验证状态

### 3. 更新 `docs/README.md` 索引（knowledge_base 增加两行）

### 4. commit + push